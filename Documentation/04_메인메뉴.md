# 메인 메뉴 (MainMenu)

## 한 줄 요약

`MainMenu` 씬은 **4인 로비·카탈로그 선택·라운드 시작**을 담당하는 부스 운영 허브다. 화면은 **좌 65% / 우 35% / 하단 20%** (`Panel_Left` · `Panel_Right` · `Slot_Player`). 하단은 포커 카드 HUD(`SlotPokerHud`). 목록 비주얼(릴)은 아직 없고, ↑/↓는 우측 TMP만 갱신한다.

---

## 화면 구성 (UI)

스크린샷 기준이 아니라 **현재 씬 Hierarchy**다.

```text
Canvas
├── Panel_Left          (x 0~0.65, y 0.2~1)   목록·릴 자리, 자식 없음
├── Panel_Right         (x 0.65~1, y 0.2~1)   TMP_DetailTitle / TMP_DetailBody
└── Slot_Player         (y 0~0.2, 가로 전체)
    ├── Slot_Player_1
    ├── Slot_Player_2
    ├── Slot_Player_3
    └── Slot_Player_4

GameSystems, PartySession, Main Camera, EventSystem
```

### 영역별 설명

| 영역 | 씬 오브젝트 | 역할 |
|------|-------------|------|
| UI 루트 | `Canvas` | Screen Space Overlay (`menuCanvas`) |
| 좌 65% | `Panel_Left` | 릴/목록용. 비어 있음 |
| 우 35% | `Panel_Right` | `TMP_DetailTitle`, `TMP_DetailBody` |
| 하단 20% | `Slot_Player` | 프리팹 `Assets/Mainmenu/Prefabs/Slot_Player.prefab` ×4 (`Slot_Player_1`~`_4`) |
| 시스템 | `GameSystems` | `GameFlowDirector` |
| 세션 | `PartySession` | DontDestroyOnLoad |

### 게임 목록 (현재)

목록 **비주얼은 없음**. ↑/↓는 `_selectedCatalogIndex`만 wrap하고 우측 TMP를 갱신한다. `MainMenuReelController` / `ReelColumn` / `ReelSymbolView` C#은 컴파일용으로 **남아 있으나 씬에서 쓰지 않음**. 재도입은 설계 잠금 후.

### 상세 패널 (우측)

선택된 `GameCatalogEntry`의 `title`·`blurb`. 필드: `id`, `title`, `blurb`, `icon`, `accentColor`.

- **제목**: `OIIA (Rhythm / Sustain)`
- **본문**:
  - `X = O, A = I, Y = A`
  - `Repeat pattern: oiiaiooiiiai.`
  - `Fail FX and rising difficulty.`

### 플레이어 슬롯 HUD (하단 4칸)

TMP `SL1_Line1`~`3`는 **사용하지 않음**(프리팹에서 비활성). Canvas 2D 카드 HUD. 스크립트 `SlotPokerHud`. `GameFlowDirector.slotPokerHud[0]`~`[3]`에 각 `Slot_Player_1`~`_4`. **JOIN/READY 카드 애니 Play 확인됨**(2026-08-31).

입력 상태머신(`EMPTY`→`ACTIVE`↔`READY`)은 기존과 같다. HP 0 EMPTY에서도 Start로 **재JOIN 가능**.

| 비주얼 | 조건 | 연출 |
|--------|------|------|
| 딤 | `EMPTY` 이고 `HP > 0` | `dimOverlay` 활성. 카드 Hidden |
| 암전 | `HP ≤ 0` | `blackoutOverlay` 활성. 카드 Hidden. Start 잠금 없음 |
| 부채꼴 `Fan` | 남은 HP 장 | 테두리 **위**. Animator `Mode=1`. 클립에서 떠오름·루프 |
| 뒷면 `FaceDown` | 잃은 장 `L = StartingHp - HP` | 테두리 `landPads[0 .. L-1]` 왼쪽부터. `Mode=2`, `Pad=i` |
| 착지 `Slam` | `READY` (및 PLAYING/RESULT) 이고 HP≥1 | 다음 빈 칸 `landPads[L]`에 앞면 1장. `Mode=3` |

Animator Int 파라미터: `Mode` (0 Hidden, 1 Fan, 2 FaceDown, 3 Slam), `Pad` (0 왼쪽, 1 오른쪽). 위치·각도는 **클립**. `Card_0`은 `SlotPokerCard`. `Card_1`은 **Animator Override Controller** `SlotPokerCard_Right`. `FaceDown`/`Hidden`은 공유.

**연승 칩**: 수직 탑 없음. 1연승당 1장, 표시 최대 `streakChips.Length`(5). Inspector `[0]`=오른쪽 `Chip_1`, `[1]`=그 왼쪽 `Chip_2` … `[4]`=`Chip_5`. `EMPTY`·HP≤0·연승 0이면 전부 끔. 6연승 이상은 5장만. 스프라이트 `Assets/Mainmenu/Sprites/Chips.png` 슬라이스 `Chip_1`~`Chip_5`. 배치는 에디터, 코드는 `SetActive`.

에셋: 앞면 `Assets/Mainmenu/Sprites/Cards/card fronts/hearts/ace of hearts.png`, 뒷면 `card backs/card back red.png`, 테두리 `Cards/card_outline.png` ×2(가로).

---

## 역할과 책임

| 구성요소 | 파일·씬 | 역할 |
|----------|---------|------|
| `GameFlowDirector` | `Assets/_Project/Scripts/Flow/GameFlowDirector.cs` | 로비 START, 카탈로그 ↑/↓·Enter, 미니게임 씬 로드, `slotPokerHud` Apply |
| `SlotPokerHud` | `Assets/_Project/Scripts/Flow/SlotPokerHud.cs` | 카드 Mode/Pad·딤/암전·연승 칩 on/off |
| `GameCatalogEntry` | `Assets/_Project/Scripts/Flow/GameCatalogEntry.cs` | 카탈로그 항목 |
| `MainMenuReelController` 등 | 동 폴더 | **미사용**. 설계 후 폐기 또는 교체 |
| `PartySession` | `Assets/_Project/Scripts/Flow/PartySession.cs` | DontDestroyOnLoad. HP·연승·연습 큐·Result 복귀 |
| `PlayerSlotModel` | `Assets/_Project/Scripts/Core/PlayerSlotModel.cs` | 슬롯 상태 머신, JOIN/READY 토글 |
| `OperatorInputService` | `Assets/_Project/Scripts/Input/OperatorInputService.cs` | 운영자 키보드 (↑↓/WS, Enter) |
| `SlotGamepad` | `Assets/_Project/Scripts/Input/SlotGamepad.cs` | 슬롯별 패드 START |
| 씬 | `Assets/Scenes/MainMenu.unity` | UI 배치·Inspector 직렬화 |

메인 메뉴는 **로비·런처**이며, 미니게임 본편은 `Minigame_O.I.I.A.` 등 **별도 씬**에서 구동된다.

---

## 입력

### 운영자 (키보드)

| 입력 | 동작 |
|------|------|
| `↑` | 카탈로그 인덱스 **위** (순환). 우측 TMP만 갱신 |
| `↓` | 카탈로그 인덱스 **아래** (순환) |
| `Enter` / 키패드 Enter | **전원 READY**일 때 선택 게임 시작 |

`OperatorInputService`는 **새 Input System**과 **레거시 `UnityEngine.Input`** 을 모두 확인한다.

### 플레이어 (패드, 슬롯 0~3)

| 슬롯 상태 | START |
|-----------|-------|
| `EMPTY` | **JOIN** — HP를 `PartySession.StartingHp`로 설정, `WinStreak = 0`, `ACTIVE` |
| `ACTIVE` | **READY** 토글 |
| `READY` | **ACTIVE**로 되돌리기 (토글) |

**게임 중·Result 씬 중에는 JOIN 불가.** `EMPTY → ACTIVE` 전환은 메인 메뉴(`TickMenuLobby`)에서만 허용된다.

### 시작 조건 (`CanStartFromMenu`)

- **한 명이라도 `ACTIVE`만** 있으면 Enter **불가** → 참가 중인 모든 슬롯이 **`READY`** 여야 한다.
- **최소 1명 `READY`** 필요.

---

## 플레이어 플로우

```text
[메인] EMPTY ──START──► ACTIVE ──START──► READY
                              ▲              │
                              └── START ─────┘ (토글)

[운영자 Enter, 전원 READY]
        │
        ▼
[Oiia 씬] 연습(기본) ──(연습 종료 후 큐)──► 본게임 1회
        │
        ▼
[Results 씬] 등수·HP·GAME OVER 연출
        │
        ▼ (운영자 Enter, FinalizeLobbyAfterMinigame)
[메인 복귀]
  · HP > 0  → ACTIVE (재-JOIN 없이 START로 READY 가능)
  · HP ≤ 0  → EMPTY (PRESS START TO JOIN)
  · HP 미감소 → WIN streak +1
```

### Oiia 연습 → 본게임 큐

- 메뉴에서 Enter로 진입하면 **기본 연습 라운드** (`TakeOiiaNextRoundIsPractice()` → `true`).
- 연습 종료 후 `PartySession.QueueOiiaMainRoundAfterPracticeEnded()` — **다음 Enter는 본게임 1회**.
- 본게임 종료 후 `ResetOiiaCycleAfterMainSession()` — 다시 **연습부터** 시작.

### Results 복귀

| 복귀 경로 | 메인에서의 동작 |
|-----------|-----------------|
| **Results 씬** (`TryConsumePostResultReport`) | 로비만 복구. 메인 오버레이 없음 |
| **레거시 직통** (`TryConsumePendingReport`) | `FinalizeLobbyAfterMinigame`만. 텍스트 결과창 **없음** |

정상 플로우는 **Results 씬 경유**.

---

## 게임 카탈로그

`GameFlowDirector` Inspector의 `catalog` 배열, 또는 비어 있을 때 코드 **폴백** (`EnsureCatalogPopulatedFallback`).

| id | title | 구현 |
|----|-------|------|
| `oiia` | OIIA (Rhythm / Sustain) | **구현됨** → `Minigame_O.I.I.A.` |
| `rhythm_button_challenge` | Rhythm Button Challenge | **구현됨** → `Minigame_RhythmButtonChallenge` |
| `coffin_dance` | 관짝춤 | **C# 완료** · 씬 `Minigame_CoffinDance` 조립·Build 후속 · blurb 추후 |
| `placeholder_04` ~ `_08` | GAME 04~08 (TBD) | **미구현** |

### 게임 시작 라우팅 (`StartSelectedMinigame`)

```text
debugRouteAllToOiia == true  OR  entry.id == "oiia"
  → oiiaSceneName

entry.id == "rhythm_button_challenge"
  → rhythmButtonChallengeSceneName

entry.id == "coffin_dance"
  → coffinDanceSceneName ("Minigame_CoffinDance") · 연습 큐 TakeCoffinDanceNextRoundIsPractice

그 외
  → detailBody "not available yet"
```

**현재 씬 설정**: `debugRouteAllToOiia = false`(2026-07-22 기준). 카탈로그 id별 씬 분기.

시작 시 `menuCanvas`를 **비활성화**한 뒤 미니게임 씬을 Single 모드로 로드한다.

---

## 구현 진행도

| 영역 | 상태 | 비고 |
|------|------|------|
| 로비 JOIN · READY · Enter | **완료** | `TickMenuLobby` (HP 0 재JOIN 유지) |
| 화면 분할 65:35 + 하단 20% | **에디터 완료** | `Panel_Left` / `Panel_Right` / `Slot_Player` |
| 카탈로그 ↑/↓ (TMP 상세만) | **완료** | `Panel_Left` 비주얼 없음 |
| 포커 카드 HUD C# | **코드 완료** | `SlotPokerHud` · `slotPokerHud` |
| 카드 프리팹·Animator·Override | **에디터 완료** · **Play 확인** | `Card_1` → `SlotPokerCard_Right` (`Assets/Mainmenu/Animation/`) |
| 칩 HUD | **에디터 완료** · **Play 확인** | `streakChips` 가로 1장·최대 5 |
| 릴·셔플 | **미착수** | 설계 남은 항목 |

**UI 성숙도**: 기능 검증용 **프로토타입**. 부스 운영에 필요한 **정보 전달·입력**은 동작하지만, 비주얼·사운드·브랜딩 연출은 후속 작업 대상이다.

---

## 씬·Inspector 기본값 (`MainMenu.unity`)

| 필드 | 씬 YAML 값 | 코드 기본값 | 비고 |
|------|------------|-------------|------|
| `PartySession.startingHp` | **2** | 3 | JOIN 시 HP |
| `GameFlowDirector.oiiaSceneName` | `Minigame_O.I.I.A.` | `Minigame_Oiia` | Build Settings 이름과 일치 필요 |
| `GameFlowDirector.debugRouteAllToOiia` | **false** | true | id별 씬 분기 |
| `GameFlowDirector.catalog` | 8항목(일부 채워짐) | 폴백 8 | `icon`·`accentColor` |
| `GameFlowDirector.reelController` | **필드 삭제** | — | |
| `PartySession.resultSceneName` | **Results** | `"Result"` | 씬 파일명과 일치 |
| `GameFlowDirector.slotPokerHud[0~3]` | **연결됨** (Play 확인) | 구 `slotHud` TMP 필드 삭제 |

---

## 코드 ↔ UI 바인딩

`GameFlowDirector` Inspector 연결 요약:

| SerializeField | 씬 오브젝트 |
|----------------|-------------|
| `menuCanvas` | `Canvas` |
| `detailTitle` | `TMP_DetailTitle` (`Panel_Right`) |
| `detailBody` | `TMP_DetailBody` (`Panel_Right`) |
| `slotPokerHud[0~3]` | 각 `Slot_Player_1`~`_4`의 `SlotPokerHud` |
| `streakChips[0~4]` | 프리팹 칩 Image. `[0]`=오른쪽 `Chip_1` (**연결·Play 확인**) |
| `partySession` | `PartySession` |

슬롯 HUD는 `PlayerSlotModel.StateChanged`와 메뉴 `Update`의 `RefreshSlotHud()` → `SlotPokerHud.Apply`. TMP 펄스는 없음.

---

## 알려진 한계·후속 작업

1. **카탈로그 placeholder** — GAME 04~08 등 미구현 항목.
2. **`debugRouteAllToOiia`** — 출시 전 false 유지, id별 씬 매핑.
3. **목록 비주얼** — `Panel_Left`가 비어 있음. 릴은 설계 후.
4. **하단 HUD** — 카드·연승 칩 **Play 확인**. 수직 탑 없음. 릴은 `Panel_Left` 후속.
5. **릴 C#** — 씬 미연결.

### 새 미니게임 추가 시 메인 메뉴 작업

1. `GameCatalogEntry`에 `id`, `title`, `blurb` (및 아이콘이 필요하면 `icon`/`accentColor`) 등록.
2. `StartSelectedMinigame`에 id → 씬 이름 분기 추가.
3. `debugRouteAllToOiia`는 false로 둔다.

---

## 관련 문서

| 문서 | 내용 |
|------|------|
| `01_프로젝트_개요.md` | 전체 아키텍처·Oiia·Results 요약 |
| `02_개발_진행_일지.md` | 메인 복귀 슬롯 유지·Results 연동 타임라인 |
| `03_Booth_USB_Controller_매핑.md` | 패드 START·Oiia 버튼 매핑 |

---

문서 갱신: **2026-09-01** (연승 칩 Play 확인)
